package main;
import java.util.ArrayList;
import java.util.List;


public class Main {
    public static void main(String[] args) {
//        // Example
//        String code = """
//        LET x = 10
//        PRINT x
//        """;
//
//        Tokenizer tokenizer = new Tokenizer();
//        Parser parser = new Parser();
//
//        // Iterate through each line of the input BASIC code.
//        for (String line : code.split("\n")) {
//
//            List<String> tokens = tokenizer.tokenize(line); // Tokenize the current line of BASIC code.
//
//
//            Statement1 statement = parser.parse(tokens); // Parse the tokens
//
//
//            System.out.println(statement); // Print the parsed thing
//            
//            List<Statement> program = new ArrayList<>();
//            program.add(new AssignmentStatement(statement[0], 2));
//            program.add(new PrintStatement("X"));
//
//            // Execute the program
//            Executor executor = new Executor();
//            executor.execute(program);
//        }
//
    	        // Example BASIC code (variable assignment)
    	        String program = "LET A = 10\n" +
    	                         "LET B = A\n";

    	        // Step 1: Parse the code into a list of statements (Front-end)
    	        Parser parser = new Parser();
    	        List<Statement> statements = parser.parse(program);

    	        // Step 2: Execute the statements (Back-end)
    	        Executor executor = new Executor();
    	        executor.execute(statements);
    	    
        
    }
}

